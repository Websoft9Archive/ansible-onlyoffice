# This vars is only for ONLYOFFICE Document Server which will cover the follow vars for ONLYOFFICE
region: "0"
common_install_docker: True
docker_install: False
mysql_install_server: False

nginx_appname: "onlyofficedocs"
nginx_vhost_mode: "reverse"
nginx_reverse_proxy_port: "9002"
nginx_certbot: True

docker_applications: 
  - onlyofficedocs
